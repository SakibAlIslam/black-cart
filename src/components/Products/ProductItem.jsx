import React from 'react'
import styled from 'styled-components'

const Container = styled.div`
flex:1;
margin:8px;
height: 75vh;
position:relative;
`

const ProductItem = ({item}) => {
  return (
    <Container>ProductItem</Container>
  )
}

export default ProductItem